package rsvier.workshop;

import java.sql.*;

import java.util.logging.*;
/*
import rsvier.workshop.dao.*;
import rsvier.workshop.domain.*;
import rsvier.workshop.domain.Customer.CustomerBuilder;
import rsvier.workshop.utility.*;

*/
public class Test {
	/*
	
	private static Logger logger = LogConnection.getLogger();

	public static void main(String[] args) {
		
		try (Connection conn = DatabaseConnectionXML.getConnection();){
			
			Address address = new Address.AddressBuilder().streetName("Bakemakade").houseNumber(108).postalCode("3000ME").city("Rotterdam").country("Netherlands").build();
			
			Customer customer  =  (Customer) new Customer.CustomerBuilder().customerId(1).name("Evy").lastName("Wetch").middleName("Evyvy").email("evy@gmail.com").address(address).build();
			
			

			
			AddressDAO adi = new AddressDAOImp();
			adi.createAddress(address, customer);
			
			
			
		} catch (SQLException e) {
			logger.log(Level.WARNING, "Can't connect to the databse", e.getMessage());

		}

	}
	*/

}
